// prisma/schema.prisma

generator client {
  provider = "prisma-client-js"
  output   = "./generated/client"
}

datasource db {
  provider = "sqlserver"
  url      = env("DATABASE_URL") // Замените на вашу строку подключения
}

model person {
  ID      Bytes    @unique
  Name    String
  Descr   String
  OSName  String?
  Changed DateTime
  RolesID Int      @default(8)
  Show    Bytes
  Data    Bytes
  EAuth   Bytes
  AdmRole Bytes
  UsSprH  Int
}

model v8users {
  ID      Bytes    @unique
  Name    String
  Descr   String
  OSName  String?
  Changed DateTime
  RolesID Int      @default(8)
  Show    Bytes
  Data    Bytes
  EAuth   Bytes
  AdmRole Bytes
  UsSprH  Int
}
